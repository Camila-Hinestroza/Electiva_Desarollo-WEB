import React from 'react';

const ImagenDetalles = ({ photographer}) => {
    return (
        <div className="imagen-detalles">
            <p>Fotógrafo: {photographer}</p>
        </div>
    );
};

export default ImagenDetalles;
